const BASE_URL=import.meta.env.VITE_API_URL||'http://localhost:8080/api';
export async function api(path,options={}){const token=localStorage.getItem('ft_token');const headers={...(options.body?{'Content-Type':'application/json'}:{}),...(token?{Authorization:`Bearer ${token}`} :{}),...options.headers};let response;try{response=await fetch(BASE_URL+path,{...options,headers});}catch{throw new Error('Không thể kết nối backend tại localhost:8080');}const json=await response.json().catch(()=>({}));if(response.status===401||(response.status===403&&path.startsWith('/admin/'))){localStorage.removeItem('ft_token');localStorage.removeItem('ft_user');window.dispatchEvent(new Event('fasttravel:session-expired'));throw new Error('Phiên đăng nhập quản trị đã hết hạn. Vui lòng đăng nhập lại bằng tài khoản Admin.')}if(!response.ok)throw new Error(json.message||`Máy chủ trả lỗi ${response.status}`);return json.data;}
export const money=value=>Number(value||0).toLocaleString('vi-VN')+' ₫';
export const date=value=>value?new Date(value).toLocaleString('vi-VN',{dateStyle:'short',timeStyle:'short'}):'—';
